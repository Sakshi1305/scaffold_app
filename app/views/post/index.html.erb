<h1>Listing posts</h1>
 
<table>
  <tr>
  	<th>User ID</th>
    <th>Title</th>
    <th>Body</th>
  </tr>
 
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.user_id %></td>
      <td><%= post.title %></td>
      <td><%= post.body %></td>
    </tr>
  <% end %>
</table>
